/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['about.x.com', 'i.pinimg.com', 'img.daisyui.com', 'nfty-temp.s3.us-west-1.amazonaws.com'],
    },
};

export default nextConfig;